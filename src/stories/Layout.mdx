import { Meta, Title, Description, Canvas, Story, Controls, ArgTypes } from "@storybook/blocks";
import * as StackStories from "./Stack.stories";
import * as CardStories from "./Card.stories";
import * as DividerStories from "./Divider.stories";
import * as SeparatorStories from "./Separator.stories";
import * as TabsStories from "./Tabs.stories";
import * as AccordionStories from "./Accordion.stories";
import * as TableStories from "./Table.stories";
import * as StatStories from "./Stat.stories";
import * as StepsStories from "./Steps.stories";
import * as ButtonStories from "./Button.stories";

<Meta title="Layout" />

<Title>Layout Components</Title>

Components for structuring page content.

---

## Stack

Flexbox layout with spacing, alignment, and direction control. Includes `VStack` (vertical) and `HStack` (horizontal) shortcuts.

<Canvas>
  <Story of={StackStories.HStackDefault} />
</Canvas>

<ArgTypes of={StackStories} />

---

## Card

Container component with optional header, body, and footer sections.

<Canvas>
  <Story of={CardStories.Default} />
</Canvas>

<ArgTypes of={CardStories} />

---

## Divider

Separates content with a horizontal or vertical line, with optional labels.

<Canvas>
  <Story of={DividerStories.Default} />
</Canvas>

<ArgTypes of={DividerStories} />

---

## Separator

Lightweight separator between sections or items.

<Canvas>
  <Story of={SeparatorStories.Default} />
</Canvas>

<ArgTypes of={SeparatorStories} />

---

## Tabs

Organizes content into switchable tab panels.

<Canvas>
  <Story of={TabsStories.Default} />
</Canvas>

<ArgTypes of={TabsStories} />

---

## Accordion

Collapsible sections for toggling content visibility.

<Canvas>
  <Story of={AccordionStories.Default} />
</Canvas>

<ArgTypes of={AccordionStories} />

---

## Table

Data table with sortable headers, rows, and cells.

<Canvas>
  <Story of={TableStories.Default} />
</Canvas>

<ArgTypes of={TableStories} />

---

## Stat

Displays a statistic with label, value, and optional trend indicator.

<Canvas>
  <Story of={StatStories.Default} />
</Canvas>

<ArgTypes of={StatStories} />

---

## Steps

Multi-step progress indicator for wizards and onboarding flows.

<Canvas>
  <Story of={StepsStories.Default} />
</Canvas>

<ArgTypes of={StepsStories} />
