import { Meta, Title, Description, Canvas, Story, Controls, ArgTypes } from "@storybook/blocks";
import * as ButtonStories from "./Button.stories";
import * as IconButtonStories from "./IconButton.stories";
import * as InputStories from "./Input.stories";
import * as TextareaStories from "./Textarea.stories";
import * as SelectStories from "./Select.stories";
import * as CheckboxStories from "./Checkbox.stories";
import * as RadioStories from "./Radio.stories";
import * as RadioGroupStories from "./RadioGroup.stories";
import * as SwitchStories from "./Switch.stories";
import * as SliderStories from "./Slider.stories";
import * as SegmentedControlStories from "./SegmentedControl.stories";

<Meta title="Forms" />

<Title>Form & Input Components</Title>

Components for building forms and collecting user input.

---

## Button

Buttons trigger actions. Supports variants, sizes, loading state, and icons.

<Canvas>
  <Story of={ButtonStories.Solid} />
</Canvas>

<ArgTypes of={ButtonStories} />

---

## IconButton

Icon buttons are round buttons that contain only an icon. Useful for compact toolbars and actions.

<Canvas>
  <Story of={IconButtonStories.Default} />
</Canvas>

<ArgTypes of={IconButtonStories} />

---

## Input

Text inputs for collecting user data. Supports multiple variants, sizes, labels, and validation states.

<Canvas>
  <Story of={InputStories.Default} />
</Canvas>

<ArgTypes of={InputStories} />

---

## Textarea

Multi-line text input for longer content. Supports resizing, variants, and validation states.

<Canvas>
  <Story of={TextareaStories.Default} />
</Canvas>

<ArgTypes of={TextareaStories} />

---

## Select

Dropdown select component for choosing from predefined options.

<Canvas>
  <Story of={SelectStories.Default} />
</Canvas>

<ArgTypes of={SelectStories} />

---

## Checkbox

Checkboxes allow users to select one or more items from a set.

<Canvas>
  <Story of={CheckboxStories.Default} />
</Canvas>

<ArgTypes of={CheckboxStories} />

---

## Radio

Radio buttons allow users to select a single option from a set.

<Canvas>
  <Story of={RadioStories.Default} />
</Canvas>

<ArgTypes of={RadioStories} />

---

## RadioGroup

A convenience wrapper around radio buttons with support for horizontal layout and descriptions.

<Canvas>
  <Story of={RadioGroupStories.Default} />
</Canvas>

<ArgTypes of={RadioGroupStories} />

---

## Switch

Toggle switches for binary on/off states.

<Canvas>
  <Story of={SwitchStories.Default} />
</Canvas>

<ArgTypes of={SwitchStories} />

---

## Slider

Sliders let users select a value from a range by dragging a handle.

<Canvas>
  <Story of={SliderStories.Default} />
</Canvas>

<ArgTypes of={SliderStories} />

---

## SegmentedControl

A set of mutually exclusive options displayed as a segmented button group.

<Canvas>
  <Story of={SegmentedControlStories.Default} />
</Canvas>

<ArgTypes of={SegmentedControlStories} />
