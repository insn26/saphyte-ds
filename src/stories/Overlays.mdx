import { Meta, Title, Description, Canvas, Story, Controls, ArgTypes } from "@storybook/blocks";
import * as ModalStories from "./Modal.stories";
import * as DrawerStories from "./Drawer.stories";
import * as PopoverStories from "./Popover.stories";
import * as TooltipStories from "./Tooltip.stories";

<Meta title="Overlays" />

<Title>Overlay Components</Title>

Components for displaying content above the page surface — modals, drawers, popovers, and tooltips.

---

## Modal

Modal dialogs display content that requires user attention or interaction. Composed of `Modal`, `ModalContent`, `ModalHeader`, `ModalBody`, `ModalFooter`, and `ModalCloseButton`.

<Canvas>
  <Story of={ModalStories.Default} />
</Canvas>

<ArgTypes of={ModalStories} />

---

## Drawer

Drawers slide in from the edge of the screen. Composed of `Drawer`, `DrawerContent`, `DrawerHeader`, `DrawerBody`, `DrawerFooter`, and `DrawerCloseButton`.

<Canvas>
  <Story of={DrawerStories.Default} />
</Canvas>

<ArgTypes of={DrawerStories} />

---

## Popover

Popovers display floating content relative to a trigger element. Composed of `Popover`, `PopoverTrigger`, `PopoverContent`, `PopoverHeader`, `PopoverBody`, and `PopoverFooter`.

<Canvas>
  <Story of={PopoverStories.Default} />
</Canvas>

<ArgTypes of={PopoverStories} />

---

## Tooltip

Tooltips display a brief informational label when users hover over or focus an element.

<Canvas>
  <Story of={TooltipStories.Default} />
</Canvas>

<ArgTypes of={TooltipStories} />
